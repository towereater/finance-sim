# Server
server:
  port: "12000"

# DB connection
db:
  host: "mongo:27017"
  timeout: 5

# Collections
prefix: "US"
collections:
  users: "users"

# External services
services:
  security: "security:12000"
  timeout: 5
