# Server
server:
  port: "12000"

# DB connection
db:
  host: "mongo:27017"
  timeout: 5

# Collections
prefix: "AC"
collections:
  accounts: "accounts"

# External services
services:
  users: "user-service:12000"
  timeout: 5
